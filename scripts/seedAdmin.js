import 'dotenv/config'; import bcrypt from 'bcryptjs'; import { connectDb } from '../src/db.js'; import { User } from '../src/models.js'; await connectDb(); const email = process.env.ADMIN_EMAIL, password = process.env.ADMIN_PASSWORD; if (!email || !password) throw new Error('Set ADMIN_EMAIL and ADMIN_PASSWORD in .env'); await User.findOneAndUpdate({ email: email.toLowerCase() }, { email: email.toLowerCase(), role: 'admin', passwordHash: await bcrypt.hash(password, 12) }, { upsert: true }); console.log(`Admin ready: ${email}`); process.exit();
